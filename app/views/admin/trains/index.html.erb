<div class="page-header">
  <h1><%= t('.title') %> <small></small></h1>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= t('.number') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trains.each do |train| %>
      <%= render train %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('common.add'), new_admin_train_path, class: "btn btn-primary", role: "button" %><br />

