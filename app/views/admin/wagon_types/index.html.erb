<div class="page-header">
  <h1><% t('.title') %> <small></small></h1>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= t('admin.wagon_types.attributes.name') %></th>
      <th><%= t('admin.wagon_types.attributes.type') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @wagon_types %>
  </tbody>
</table>

<br>

<%= link_to t('common.add'), new_admin_wagon_type_path, class: "btn btn-primary" %><br />

