<div class="page-header">
  <h1><%= t('.title') %> <small></small></h1>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= t('.name') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @routes.each do |route| %>
      <%= render route %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('common.add'), new_admin_route_path, class: "btn btn-primary" %><br />

