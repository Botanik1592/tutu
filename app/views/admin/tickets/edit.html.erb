<div class="page-header">
  <h1><%= t('.title') %> <small></small></h1>
</div>

<%= render 'form', ticket: @ticket %>

<div class="row">
  <div class="col-md-2">
    <%= link_to t('common.show'), [:admin, @ticket], class: "btn btn-info" %>
  </div>
  <div class="col-md-2">
    <%= link_to t('common.back'), admin_tickets_path, class: "btn btn-primary" %>
  </div>
</div>
